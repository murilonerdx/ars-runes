Esta pasta usa Gradle 8.8.

Se `gradle-wrapper.jar` não estiver presente:
- Windows: rode `download-wrapper.bat`
- Linux/macOS: rode `./download-wrapper.sh`

Depois:
1. Abra a pasta `mod` na IDE
2. Use **Gradle Wrapper**
3. Rode `gradlew.bat genIntellijRuns`
4. Rode `gradlew.bat build`
